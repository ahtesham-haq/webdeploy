name: Run Script with Admin Rights

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run PowerShell script as Administratorwww
        shell: pwsh
        run: |
          Start-Process -FilePath "pwsh" -ArgumentList "-ExecutionPolicy Bypass -File 'D:\Ahtesham ul haq\Projects\Learning\Github\webdeploy\WebApplicationMy\deploy.ps1'" -Verb RunAs -Wait
